#include "holberton.h"

/**
 * set_string - a function that sets the value of
 * a pointer to a char.
 * @s: takes the value of a pointer
 * @to: takes the value of an int
 * Return: a char
 */
void set_string(char **s, char *to)
{
*s = to;
}
