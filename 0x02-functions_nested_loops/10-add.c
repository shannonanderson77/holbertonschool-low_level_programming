#include "holberton.h"

/**
  * add - a function that adds two integers
  * and returns the result
  * @x: first integer
  * @y: second integer
  * Return: result of int and int
  */
int add(int x, int y)
{
	return (x + y);
}
